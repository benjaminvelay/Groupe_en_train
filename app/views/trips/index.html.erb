<div class="container">
  <div class="row">
    <div class="col-sm-8 col-md-6 col-lg-6 col-offset-4">
      <% @trips.each do |trip| %>
      <%= trip.departure_at %>
      <%= trip.arrival_at %>
      <%= trip.station_departure %>
      <%= trip.station_arrival %>
      <%= trip.individual_price %>
      <%= trip.group_price_per_user %>
    </div>
  </div>
</div>
