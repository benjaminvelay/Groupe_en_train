
<div class="container">
  <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
    <div class="flex-station-start-end">
      <h3>Votre trajet
      <%= @trips.first.departure_station %>
      <i class="fas fa-arrow-right"></i>
      <%= @trips.first.arrival_station %>
      </h3>
      <div class="info-trip">
        <p>
          <%= @trips.first.departure_date.to_date.strftime('%a %d %b %Y') %>
        </p>
        <p>Tarifs individuels</p>
        <p>Tarifs groupés</p>
      </div>
    </div>
    <% @trips.each do |trip| %>
      <div class="flex-card">
        <div class="flex-heure-start-end">
          <p><%= trip.departure_station %></p>
          <i class="fa fa-arrow-right"></i>
          <p><%= trip.arrival_station%> </p>
          <p><%= trip.departure_date %> </p>
        </div>

          <p><%= trip.total_price %>€</p>
          <p class="click-tarif-or"><%= trip.individual_price %>€</p>
      </div>
    <% end %>
  </div>
</div>
<!-- <div class="col-md-6 col-sm-6 col-left">
            <div class="line-degrade">
              <div class="line">
                <span class="station-dep">07h04</span>
                <span class="station-arr">09h08</span>
              </div>
              <div class="line-deg">
                <div class="duration">2H04</div>
              </div>
              <div class="line">
                <span class="station-dep">Saint Jean</span>
                <span class="station-arr">Montparnasse</span>
              </div>
            </div>
            <div class="cont-dest">
              <div class="cont-stations">

                <p class="train-infos">TGV 8402 - Direct - classe 2</p>
              </div>
            </div>
          </div>
 -->
<!-- <div class="container">
  <% @trips.each do |trip| %>
    <div class="row">
      <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
        <div class="row">
          <%= @trips.first.station_departure %>
          <i class="fas fa-arrow-right"></i>
          <%= @trips.first.station_arrival %>
          <p> <%= trip.individual_price.to_s.gsub(".",",") %>€ </p>
          <p class="click-tarif-or"> <%= trip.group_price_per_user_cents.to_s.gsub(".",",") %>€ </p>
        </div>
        <div class="row">
          <%= (trip.arrival_at.hour - trip.departure_at.hour)%>h
          <%=trip.arrival_at.min - trip.departure_at.min %>
        </div>
      </div>
    </div>
  <% end %>
</div>
 -->
